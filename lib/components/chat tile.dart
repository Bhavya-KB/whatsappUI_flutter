import 'package:flutter/material.dart';

class ChatTile extends StatelessWidget {
final String name;
final String img;
final String msg;
 final String time;

 ChatTile({required this.name,required this.img,required this.msg,required this.time});

  @override
  Widget build(BuildContext context) {
    return  Container(

    );
  }
}